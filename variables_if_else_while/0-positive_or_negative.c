#include <stdlib.h>
#include <time.h>

gcc -o positive_or_negative
-Wall | -Werror | -Wextra | -pedantic
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	std=gnu89 0-positive_or_negative.c 
	return (0);
}
